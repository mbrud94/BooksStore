@model IEnumerable<BooksStore.Models.Category>

<ul class="dropdown-menu">
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name,"Index","Books", new {CategoryId = item.CategoryId }, null)</li>
        <!--<li><a href="/Books/BooksByCategory/@item.CategoryId">@item.Name</a></li>-->
    }
</ul>

